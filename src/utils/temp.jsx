/* To make text responsive inside post Use this code */
// const [fontSize, setFontSize] = useState(16);
// useEffect(() => {
//     const containerWidth = document.querySelector(".paragraph").offsetWidth;
//     const textLength = text.length;
//     setFontSize(Math.max(16, Math.min(48, (containerWidth / textLength) * 2)));
//     console.log(containerWidth, textLength,)
// }, []);